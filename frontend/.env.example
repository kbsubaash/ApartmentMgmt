# Backend API base URL — used by Vite dev proxy and production
# Development: /api (proxied via vite.config.js)
# Production: https://apartment-mgmt-backend.onrender.com/api (or your backend URL)
VITE_API_URL=/api
